- name: packages
  become: true
  become_method: sudo
  macports:
    name: "{{ ports }}"
  vars:
    ports:
      - w3m
      - vim
      - tmux
      - tree
      - nmap
      - htop
      - bash-completion
      - ossp-uuid
      - cloc
